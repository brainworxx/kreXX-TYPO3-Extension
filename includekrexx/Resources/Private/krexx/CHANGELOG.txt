1.3.2
- Bugfix: Removed that annoying "Hidden internal properties"
  message.
- Bugfix /Feature: Added a small blacklist of classname/debugfunction
  combination which may cause problems during object analysis.
- Removed the old and ugly schablon skin.

1.3.1
- Bugfix: Local open function is working again.
- Bugfix: Displaying the local configuration does not
  re-enable kreXX anymore.
- Bugfix: Emergency break does not trigger a false positive
  anymore.
- Bugfix: Display of wrong values in the settings, in case
  those settings are not editable and there are some leftover
  values in the settings cookie.
- Bugfix: Proper display of static values in objects.
- Bugfix: Proper display of internal properties of predefined
  php classes
- Removed the Debug Cookie in favor for the local open
  function

1.3.0
- Added search function to the Hans skin.
- Added collapse-everything-else to the Hans skin.
- Added better recursion handling in the Hans skin.
- Added administration for the FE config.
- Added CLI detection and message handling in CLI.
- Bugfix: Possible jQuery errors when the host site is
  using the noConfict mode.
- Bugfix: Refactored css of the Hans skin for minimal
  interference with the host template. Most base64
  images were replaced by unicode characters.
- Bugfix: Possible false string encoding.

1.2.1a and 1.2.2.a
- Renamed the skin "schablon.com" to "schablon" to prevent
  Chrome from telling the user that the download is unusual
  See https://sourceforge.net/p/krexx/bugs/11/

1.2.2
- Bugfix: Removed interaction with the Magento backend
  template.
- Bugfix: Configurable debug methods are not forgetting
  their old values after display anymore.
- Bugfix: \krexx::editSettings() is working again.

1.2.1
- Reduced overall memory usage.
- Added memory usage check during frontend rendering.
- Added check if log and chunk folders are writeable.
- Added analysis for protected and private class methods.
- Emergency break is now configureable.
- Moved output to a shutdown callback.
- Adjustments for the backend editor of the config file.
- Bugfix: The config display now resets the hive.
- Bugfix: Sourcecode in the backtrace does not display
  strange char count anymore.
- Bugfix: Configuration file get loaded again.
- Bugfix: Fatal error for a private or protected
  configured debug method
- Bugfix: Catchable error for a configured debug method
  with parameters

1.2.0
- Added Hans template.
- Added fatal error handler with a backtrace.
- Added message feedback from the configuration validation.
- Backtrace is now featuring highlighted parts of the
  sourcecode.
- Refactored code to meet Drupal conventions
- Refactored large parts of the internal configuration
- Bugfix: string length is now the right value again
- Bugfix: Added rootpath to the local cookie settings
  so they will be available on the whole site.
- Bugfix: 'Header already send' warning should not
  happen anymore, because we use output buffering, in case
  it is not used and we are the first ones to send
  something to the browser. This should also fix the
  login problems with drupal and typo3.
- Bugfix/Feature: Krexx tries now to detect whether the
  current request is ajax, preventing output which would
  break the AJAX request.
- Bugfix/Feature: Kexx now accepts an empty jsLib value.
- Bugfix: PHP warning from debug callback functions
- Bugfix: Possible timestamp collision with the logfiles

1.1.0
- Refactored naming conventions
- Bugfix: Multiple inclusion of jQuery an the template js file
- Cleaned up generated HTML code
- Added scrollbars to the html DOM element, to make sure that
  there is enough room to render kreXX
- Added closing button
- All settings are now editable. Added a function to display
  kreXX, despite some local settings to prevent a lockout.
- Added support for mixed encoding in string analysis.
- Moved getDebugCookie functionality to the local settings. 
- Bugfix: keXX should now use the included jQuery version, in
  case that there are other versions included.

1.0.1
- Added js error handling, in case JQuery fails to load
- Added support for inherited comments for class method
  analysis
- Cleaned up kreXXinternals and rearranged the class architecture
- Added a static cache in kreXXRender
- A couple of fixes for PHP 5.3
- Fixed a path issue with the template.

1.0.0
- Cleaned up js including routines and made them configurable
- Reworked the render configuration settings
- Wrote own draggable js routine and unbundled jQuery UI
- Made "deep" configuration in the footer editable. The values will
  be stored in a cookie. Now every developer can have his own local
  settings, without the need to edit the global file.
- Renamed kreXX.ini to kreXX.ini.example, so an update will not
  overwrite the project settings.
- Added a configurable local open function.
- Added help text for all configurable settings.
- Bugfix: Recursion detection for arrays now works as expected
- Added an "emergency break" to prevent timeouts and WSOD
- Added possibility to store the kreXX.ini somewhere else
- Added type of variable type to the headline
- Compatibility with IE8 and IE9

1.0.0rc3
- Added index.php and .htaccess to the logfolder.
- Refinded Drupal db logging.

1.0.0rc2
- Minimal skin adjustment for the old Magento standard template
- Added installation readme
- A click on a Recursion now jumps to the actual analysis element and
  highlights it
- Started to write this file in English, since rc1 is now available on
  Sourceforge
- Bugfix: Additional callback debug methods do not assume to get an
  array (or object) any more.
- Added function alias for \kreXX::open($data);
- Removed all HTML code from kreXXRender and moved it to real template
  files
- Removed Apache callback to clean up output stream.
- Traversable analysis now tells you, that it's empty instead of
  displaying no traversable info.
- Added configuration info to output and rearranged the template
- Reworked fallback settings, in case it is not set in the ini.
- Bugfix: Doctype will be send only once by kreXX.
- When clicking on a recursion, the <li> with the analysis data will
  now highlight in red.
- Reformatted this file, to make it better readable.

1.0.0rc1
- Objekt-Recursion-Detection implementiert.
- Umbenannt in kreXX
- Funktion analyse umbenannt in open
- Iniwerte in der Auslieferungskonfiguration zuraeckgesetzt.
- Link zur unserer Sourceforge Seite hinzugefaegt
- Warning beim Zuraecksetzen der Arrays beseitigt
- Liesmich und ToDo entfernt, da beides jetzt in der Sourceforge Wiki
  liegt.
- Legal Notices in den Dateien angepasst
- Im CSS den Mauszeiger im Dragable Bereich auf 'move' gestellt
- Installation Datei hinzugefaegt.

1.0.0b5
- Bugfix: Analyse der der Protected und Private gibt jetzt auch den
  aktuellen Wert zuraeck, und nicht nur den Standardwert.
- Generiertes DOM aufgeraeumt und doppelte </li>'s entfernt
- Bugfix: Timer wird jetzt faer unterschiedliche Zeitnahmen vollstaendig
  resettet.

1.0.0b4
- Umbenannt in kruXX
- Ausliegferung der Bilder erfolgt jetzt per Base64. Option faer den
  Bilderserver entfernt.
- Kommentarformatierung und -escaping in der Methoden-Analyse geht
  jetzt endlich so wie es sein soll
- Die Headline ist jetzt besser zu erkennen, und der Mauszeiger gibt
  jetzt zu erkennen, dass es hier eine Interaktionsmoeglichkeit gibt
  (dragable)
- In der Ini kann man jetzt verhindern, dass aeberhaupt ein Reflection
  Objekt erzeugt wird, fals das Probleme macht.
- Referenz auf alte PHP 4 Bugs entfernt, da es eh PHP 5.3 braucht
- Alte Templates entfernt.
- Quellcode weiter aufgeraeumt
- Fileheader aeberall eingefaegt
- Moeglichkeit gegeben in der Ini das Teil global zu deaktivieren

1.0.0b3
- Bugfix: Endlosschleife bei der Propertyausgabe wegen einem falschen
  Funktionsaufruf.
- Bugfix: Methodenanalyse schafft es jetzt nicht mehr aus dem DOM
  auszubrechen
- Bugfix: Kompartibilitaet zu Typo3 6.2 wieder hergestellt
- Namespace (KrumSpace) eingefaehrt, um den Root nicht unnoetig zu
  verschmutzen
- Dump von Private & Protected Properties eingebaut.
- Krumo sagt jetzt Bescheid, warum er eine Objekt-Tiefenanalyse 
  abgebrochen hat.

1.0.0b2
- Objektanalyse einklappbar gemacht.
- Bugfix: Cloning wieder rausgenommen, da bei einer gestapelten
  Objektanalyse Krumo sich sonst totgelaufen haette. Momentan ist es
  mit Magento kompartibel, da ich die Properties nicht untersuchen
  muss. In anderen Systemen kann das aber zu Problemen faehren.
- DOM aufgeraeumt
- Maximale Anzahl von Aufrufen eingefaegt. Unter VTiger werden sonst
  Dateigroessen von aeber 380 MB produziert.
- Es laesst sich jetzt einstellen, wie tief ein Objekt analysiert wird.
  Wie gesagt, im VTiger kann das mal etwas heftiger werden (no pun
  intended).
- Bugfix: In der Deep Objektanalyse bin ich immer von einem Array oder
  Objektoutput ausgegangen, weswegen ich direkt die _vars() aufgerufen
  hatte.
- Debug-Methoden fuer Objekte lassen sich jetzt in der Ini erweitern.

1.0.0b1
- Umsetzung vom MVC, soweit wie das in einem Debugtool aeberhaupt
  moeglich ist (auch wenn ich es nicht so genannt habe).
  Die Public Schnittstellen liegen in \krumo.		(Controller)
  Die "Template" Funktionen liegen in \renderKrumo. (View)
  Das ganze "Framework" liegt in \internalKrumo.	(Model)
  Damit sollte das Teil wesentlich besser zu warten und zu erweitern
  sein.
- Bugfix: variables() gibt jetzt auch wirklich die globalen Variablen
  aus.
- Bugfix: includes() gibt jetzt alle inkludierten Dateien aus.
- Bugfix: phpini() wirft keinen Fatal Error mehr
- Bugfix: get() sagt jetzt Bescheid, was aeberhaupt ausgegeben wird
- Bugfix: startDrupalLogging() hat jetzt eine Fehlerkorrektur
- Kompatibilitaet Zend Framework: RecursionMarker setzen klappt jetzt
  auch bei ZendObjekten
- Alle Analyse-Funktionen laufen jetzt im selben Output
- dumpReflection() hab ich wieder herausgenommen, die Ausgabe ist zu
  aebel. Vielleicht werde ich das Teil spaeter wieder mit reinnehmen,
  wenn ich die Zeit habe den Output zu haebschen.
- ini() gibt jetzt auch Raeckmeldung, wenn es nicht gelungen ist die
  Datei zu oeffnen.

0.10.2
- Recusion Marker Fix aeber ein Clone erledigt, das sollte wesentlich
  sauberer sein.
- Function Dump direkt unterhalb des alten Object Dumps angesiedelt.
- Function Dump um die Daten aus dem Reflection Object erweitert.
  Eventuell fehlen hier noch weitere Werte.
- Alles was vorher Private war ist nun Protected, als Vorbereitung faer
  die Auslagerung in Decorater-Klassen. Der Quellcode wird langsam
  wirklich unaebersichtlich, insbesondere der Paintbereich.

0.10.1
- Bugfix mit dem Recusion Marker und Magento. Die Magical Functions
  in einem Varien Object haben den Marker in den Debug-Array gepackt,
  mit dem Ergebnis, dass die debug() und toArray Analyse der Meinung
  war im Kreis zu laufen (Recusion).

0.10.0
- In der Ini laesst sich jetzt pflegen, ob die Ausgabe in einer Datei
  oder im Frontend ausgeben werden soll
- Endgaeltiger Bugfix faer die springende Ausgabe.
- Die Debugausgabe laesst sich cookie-abhaengig schalten. Krumo kann
  nataerlich weiterhin komplett aeber disable() abgeschaltet werden, dann
  hilft auch kein Cookie. Die vorherige Magentoimplementierung wurde
  entfernt.
- Der interne Aufrufcounter ist jetzt Private. Keine Ahnung, warum er
  jemals Public war.
- Der Standard Dump ist jetzt Private, da er bereits Teil der 
  Standard-Analyse ist.
- Es gibt endlich eine LIESMICH.
- Zeitnahme kommt jetzt auch damit klar, dass das selbe Ereignis mehrmals
  aufgerufen wird

0.9.2
- Krumo checkt, ob es eine Magentoinstallation ist, und ob der
  adminhtml cookie gesetzt ist. Wenn es ein Magento ist, aber der
  Cookie nicht da ist, wird Krumo deaktiviert.

0.9.1
- Zeitnahme hinzugefaegt (timerStart, timerMoment, timerEnd). Output
  erfolgt in einer Analyse-Datei.
  
0.9.0
- dumpRefelction() eingefaet, ist aber kein Teil der Standardanalyse.
- Es ist jetzt nur noch der Kopfbereich selber dragable, was dazu fuehert,
  dass Krumo nicht mehr hin und herspringt (Bug) und die Ergebnisse
  selber sind wieder selektierbar.

  0.8.2
- Krumo raeumt sein Logverzeichnis selbststaendig auf.
- Logverzeichnis l�sst sich jetzt in der krumo.ini einstellen

0.8.1
- Einige Systeme (z.B. VTiger) speichern br's in der DB als
  Nicht-HTML-Zeilenumbrueche. Webbrowser stellen diese normalerweise nicht
  dar, weswegen sie im Krumo unsichtbar waren. Ab sofort werden diese in
  Strings mit nl2br() umgewandelt, so das sie als solche sichtbar sind.

0.8.0
- Analyse() schaut sich jetzt zusaetzlich eine moegliche __toString()
  Funktion an
- Vorschautexte werden jetzt mit einem einfachen Linksklick selektiert
- ToDo Liste geschrieben
- Changelog angefangen
